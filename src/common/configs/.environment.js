// import dotenv from "dotenv";
// // tach ra 1 file rieng khi nao can thi import
// dotenv.config({});
// export const { PORT, HOST, MONGO_URI } = process.env;

import dotev from "dotenv";
dotev.config({});
export const { PORT, HOST, MONGO_URI } = process.env;
